# shared/realtime — reserved for Phase 6

The cross-dashboard event bus lives here (BroadcastChannel API across tabs,
or a small local WebSocket/Socket.io mock server). A trigger on the User
dashboard must show up live on the Police/Hospital dashboards.

Do not implement before Phase 6.
